a, b = 0, 2

if a<b:
    print('a ({}) is less than b ({})'.format(a,b))
else:
    print('a ({}) is not less than b ({})'.format(a,b))

# the {} replaces the string values assigned to the variables with the format function

# below is an conditional expressions
print("foo" if a<b else "bar")